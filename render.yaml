services:
  - type: worker
    name: discord-welcome-bot
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: WELCOME_CHANNEL_ID
        sync: false
      - key: WELCOME_ROLE_ID
        sync: false
